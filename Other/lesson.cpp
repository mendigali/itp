#include <iostream>
using namespace std;
int main() {

    /* 
    char var = 'A';
    cout << (int)var;
    */

    /* 
    char a = 201;
    char b = 203;
    char c = 187;
    char d = 200;
    char e = 202;
    char f = 188;
    cout << a << b << b << b << c << '\n';
    cout << d << e << e << e << f << '\n';
    */

    /* 
    int a, b, c;
    cin >> a >> b >> c;
    cout << (a + b - (double)c / a) + c * a * a - (a + b);
    */

    /* 
    int a, b;
    cin >> a >> b;
    bool answer = a % 2 == 0 || b >= 10;
    cout << answer;
    */

    /*
    int num;
    cin >> num;
    cout << "First digit: " << num / 1000 << '\n';
    cout << "Second digit: " << num / 100 % 10 << '\n';
    cout << "Third digit: " << num / 10 % 10 << '\n';
    cout << "Fourth digit: " << num % 10 << '\n';
    */

    /* 
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    if (a % 2 == 0 && d % 2 == 1) {
        swap(b, c);
    }
    cout << a << ' ' << b << ' ' << c << ' ' << d;
    */

    return 0;
}